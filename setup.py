#!/usr/bin/env python
# This file is maintained for backward compatibility with tools that don't support pyproject.toml
# The actual build configuration is in pyproject.toml

from setuptools import setup

if __name__ == "__main__":
    setup()
